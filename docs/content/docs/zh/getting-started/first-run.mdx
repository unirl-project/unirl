---
title: 首次运行
description: 组合并启动一个 UniRL 实验配方。
---

启动 Ray 作业前，先做 Hydra compose check。它能提前暴露路径、override、组件契约和 batch geometry 问题。

```bash
python -m unirl.train_diffusion --config-name=diffusion_rl/sd3_trainside --cfg job --resolve
```

## 单机运行

单机通用启动器，第一个参数是 bucket 化的 recipe 名（`<bucket>/<recipe>`）：

```bash
bash scripts/run_experiment_single_node.sh diffusion_rl/sd3_trainside
```

默认入口是 diffusion；用 `ENTRY` 选择其他入口：

```bash
ENTRY=train_vlm bash scripts/run_experiment_single_node.sh vlm_rl/argrpo_qwen_vl_geo3k_mc_4x8
ENTRY=train_pe  bash scripts/run_experiment_single_node.sh pe_rl/pe_trainside_pickscore
```

只检查启动命令，不真正跑训练：

```bash
DRY_RUN=1 bash scripts/run_experiment_single_node.sh diffusion_rl/sd3_trainside
```

## 多机运行

多机 role-aware 启动器：

```bash
bash scripts/run_experiment_multinode_taiji.sh diffusion_rl/sd3_sglang_native_colocate
```

## 直接调用 Hydra 入口

可以直接调用入口并在命令行覆盖字段：

```bash
python -m unirl.train_diffusion \
  --config-name=diffusion_rl/sd3_trainside \
  num_devices=8
```

Override 优先级：

```text
CLI Hydra override > launcher env var > YAML default
```

## 示例 prompt

已提交的 prompt 列表在 `datasets/` 下，例如 `datasets/pickscore/train.txt`（每行一个 prompt）和 `datasets/pickscore/test.txt`。recipe 的 `data_source` 默认指向它们。

真实运行时用环境变量或 CLI override 指向绝对路径：

```bash
DATA_PATH=/abs/path/train.json \
OUTPUT_DIR=/abs/path/outputs/run1 \
bash scripts/run_experiment_single_node.sh diffusion_rl/wan21_t2v
```
